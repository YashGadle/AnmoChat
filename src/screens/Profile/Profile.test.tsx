import React from 'react';

import { Profile } from '.';

describe('Home,', () => {
	let wrapper = null;
	beforeAll(() => {
		wrapper = {};
	});
	it('it should render without crashing', () => {});
});
