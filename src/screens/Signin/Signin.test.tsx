import React from 'react';

import { Signin } from '.';

describe('Home,', () => {
	let wrapper = null;
	beforeAll(() => {
		wrapper = {};
	});
	it('it should render without crashing', () => {});
});
